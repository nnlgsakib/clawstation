---
phase: 12-gateway-integration
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-03]
files_modified:
  - src/pages/dashboard.tsx
  - src/pages/install.tsx
  - src/components/layout/app-shell.tsx
  - src/hooks/use-monitoring.ts
autonomous: true
requirements:
  - GW-12
  - GW-13
  - GW-14

must_haves:
  truths:
    - "Dashboard shows real-time Gateway status via WebSocket (not polling)"
    - "Dashboard shows live agent sessions from Gateway"
    - "Dashboard shows channel connection status from Gateway"
    - "Gateway connection indicator appears in sidebar (green=connected, red=disconnected)"
    - "Install page redesigned as Gateway startup flow with real-time logs"
    - "Config changes made in Configure page are applied via Gateway hot-reload"
  artifacts:
    - path: "src/pages/dashboard.tsx"
      provides: "Live dashboard with real Gateway data"
      min_lines: 100
    - path: "src/pages/install.tsx"
      provides: "Gateway startup page with process logs"
      min_lines: 80
    - path: "src/components/layout/app-shell.tsx"
      provides: "Sidebar with Gateway connection indicator"
      contains: "GatewayStatus indicator"
  key_links:
    - from: "src/pages/dashboard.tsx"
      to: "useGatewaySessions, useGatewayConfig"
      via: "Dashboard fetches live data from Gateway WebSocket"
      pattern: "useGateway"
    - from: "src/pages/install.tsx"
      to: "start_gateway command"
      via: "Install page starts Gateway and shows output"
      pattern: "invoke.*start_gateway"
    - from: "src/components/layout/app-shell.tsx"
      to: "useGatewayConnection"
      via: "Sidebar shows connection indicator"
      pattern: "useGatewayConnection"
---

<objective>
Redesign dashboard and install page with live Gateway data

Purpose: The current dashboard shows mock data. After Gateway integration, all status data should come from the Gateway WebSocket in real-time.
Output: Live dashboard, Gateway startup page with logs, sidebar connection indicator
</objective>

<execution_context>
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/12-gateway-integration/12-CONTEXT.md
@.planning/12-gateway-integration/12-RESEARCH.md

# Existing patterns
@src/pages/dashboard.tsx
@src/pages/install.tsx
@src/components/layout/app-shell.tsx
@src/hooks/use-monitoring.ts
@src/hooks/use-gateway.ts
</context>

<interfaces>
From src/hooks/use-gateway.ts:
```typescript
export function useGatewayConnection(): { connected: boolean; connecting: boolean; error: string | null }
export function useGatewayCall<T>(method: string, params?: object): UseQueryResult<T>
export function useGatewaySessions(): UseQueryResult<AgentSession[]>
export function useGatewayConfig(): UseQueryResult<OpenClawConfig>
```

From src/components/layout/app-shell.tsx:
```typescript
export function AppShell({ children }: { children: React.ReactNode })
// Contains sidebar with navigation links
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign dashboard with live Gateway data</name>
  <files>src/pages/dashboard.tsx</files>
  <read_first>src/pages/dashboard.tsx, src/hooks/use-gateway.ts, src/hooks/use-monitoring.ts</read_first>
  <behavior>
    - Dashboard shows Gateway connection status prominently at top
    - If Gateway not connected: shows "Start Gateway" button + setup status
    - If Gateway connected: shows live data cards
    - Cards: Gateway Status (port, uptime), Agent Sessions (count + list), Active Channels (count + status badges), Sandbox Containers (count)
    - Quick actions: "Configure" link, "Channels" link, "Start Gateway" / "Stop Gateway" toggle
    - Real-time updates via Gateway WebSocket (no polling)
    - Skeleton loading while connecting
  </behavior>
  <action>
    Rewrite src/pages/dashboard.tsx:
    
    Import useGatewayConnection, useGatewaySessions, useGatewayConfig from use-gateway.ts.
    Import useGatewayStore from use-gateway-store.ts.
    
    Dashboard layout:
    1. Header: "Dashboard" title + Gateway connection badge (green "Connected" / yellow "Connecting" / red "Disconnected")
    2. If not connected and not connecting: show "Gateway Not Running" card with:
       - Description: "Start the OpenClaw Gateway to manage your AI assistant"
       - "Start Gateway" button (invokes start_gateway command)
       - Prerequisites check: Node.js installed, OpenClaw installed
    3. If connected: show grid of status cards:
       - Gateway Status card: port, connection time, config file path
       - Agent Sessions card: count, list of active sessions with model info
       - Channels card: count of enabled channels, status badges per channel
       - Sandbox card: container count, sandbox mode
    4. Quick actions row: Configure, Channels, Monitor links
    
    Replace useOpenClawStatus/useSandboxContainers/useAgentSessions with Gateway-based hooks.
    Remove polling-based queries — all data comes from Gateway WebSocket.
  </action>
  <verify>npm run build succeeds, dashboard shows real Gateway data when connected</verify>
  <done>Dashboard displays live Gateway status with real-time updates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign install page as Gateway startup flow</name>
  <files>src/pages/install.tsx</files>
  <read_first>src/pages/install.tsx, src/hooks/use-gateway.ts</read_first>
  <behavior>
    - Install page shows: prerequisites check (Node.js, OpenClaw), Gateway startup with real-time logs, connection status
    - Prerequisites: Node.js version check, OpenClaw installation check, Docker check
    - If prerequisites missing: show install buttons with progress
    - If prerequisites met: "Start Gateway" button with log viewer
    - Log viewer shows Gateway stdout/stderr in real-time (similar to Docker log viewer pattern)
    - Auto-scroll with pause-on-scroll-up
    - Connection indicator: "Connecting to Gateway..." → "Connected" → redirect to dashboard
  </behavior>
  <action>
    Rewrite src/pages/install.tsx to be the Gateway startup page:
    
    Step 1: Prerequisites Check
    - Invoke check_nodejs → show Node.js version or install prompt
    - Check if openclaw is installed (try "openclaw --version")
    - Check Docker status (reuse existing useDockerInfo)
    - Each check shows: loading spinner → success (green check) or failure (red X + install button)
    
    Step 2: Gateway Startup
    - "Start Gateway" button (only enabled when prerequisites met)
    - On click: invoke start_gateway command
    - Show log viewer with gateway-output event listener
    - Log viewer: dark terminal-style box with auto-scroll
    - Detect Gateway ready: when output contains "Gateway listening" or WS connection succeeds
    
    Step 3: Connected
    - "Gateway Connected" success state
    - Auto-redirect to dashboard after 2s
    - Or "Go to Dashboard" button
    
    Reuse the log viewer pattern from the Docker installation (scrollable div with event listener).
  </action>
  <verify>npm run build succeeds, install page shows prerequisites and Gateway startup</verify>
  <done>Install page guides user through prerequisites and Gateway startup with real-time logs</done>
</task>

<task type="auto">
  <name>Task 3: Add Gateway connection indicator to sidebar</name>
  <files>src/components/layout/app-shell.tsx</files>
  <read_first>src/components/layout/app-shell.tsx, src/hooks/use-gateway.ts</read_first>
  <action>
    In src/components/layout/app-shell.tsx:
    - Import useGatewayConnection from use-gateway.ts
    - Add a small indicator at the bottom of the sidebar:
      - Green dot + "Connected" when connected
      - Yellow dot + "Connecting" when connecting
      - Red dot + "Disconnected" when disconnected
    - Use Tailwind classes for dots: h-2 w-2 rounded-full
    - Colors: bg-green-500, bg-yellow-500, bg-red-500
    - Text: text-xs text-muted-foreground
  </action>
  <verify>npm run build succeeds, sidebar shows connection indicator</verify>
  <done>Sidebar displays Gateway connection status indicator</done>
</task>

<task type="auto">
  <name>Task 4: Update config page to use Gateway hot-reload</name>
  <files>src/pages/configure.tsx</files>
  <read_first>src/pages/configure.tsx, src/hooks/use-gateway.ts</read_first>
  <action>
    In src/pages/configure.tsx:
    - Add import for useGatewayConnection, useGatewayCall
    - If Gateway is connected:
      - Load config via useGatewayConfig() (config.get) instead of read_config command
      - On save: invoke gateway_ws_call with method="config.patch" and the changed fields
      - Show "Applied via Gateway hot-reload" success message
      - If Gateway requires restart for changes, show "Gateway restarting..." indicator
    - If Gateway is NOT connected:
      - Fall back to existing read_config/write_config file-based approach
      - Show warning: "Gateway not connected — changes will apply on next Gateway start"
  </action>
  <verify>npm run build succeeds, config save uses Gateway API when connected</verify>
  <done>Config page uses Gateway hot-reload when Gateway is running</done>
</task>

</tasks>

<verification>
- [ ] Dashboard shows real Gateway data (not mock)
- [ ] Dashboard connection status matches actual Gateway state
- [ ] Install page checks prerequisites correctly
- [ ] Install page starts Gateway and shows real-time logs
- [ ] Sidebar shows Gateway connection indicator
- [ ] Config page uses Gateway hot-reload when connected
- [ ] Config page falls back to file-based when Gateway offline
</verification>

<success_criteria>
- Dashboard displays live agent sessions from Gateway
- Dashboard shows real channel status from Gateway
- Install page detects Node.js and OpenClaw installation
- Install page starts Gateway with real-time log streaming
- Sidebar connection indicator reflects actual Gateway state
- Config changes apply via Gateway hot-reload (no restart for most settings)
- All pages gracefully handle Gateway disconnection
</success_criteria>

<output>
After completion, create `.planning/phases/12-gateway-integration/12-04-SUMMARY.md`
</output>
