---
phase: 07-installation-ux-animation-foundation
plan: "08"
type: execute
wave: 3
depends_on:
  - "07-installation-ux-animation-foundation-01"
  - "07-installation-ux-animation-foundation-02"
  - "07-installation-ux-animation-foundation-03"
  - "07-installation-ux-animation-foundation-04"
  - "07-installation-ux-animation-foundation-05"
  - "07-installation-ux-animation-foundation-06"
  - "07-installation-ux-animation-foundation-07"
files_modified:
  - src/components/install/step-install.tsx
autonomous: true
requirements:
  - INST-10
  - INST-11
  - INST-12
  - UI-01
  - UI-02
  - UI-03
  - UI-04
user_setup: []
must_haves:
  truths:
    - "Installation step shows real-time Docker log output with per-layer progress bars"
    - "Log viewer auto-scrolls but pauses when user scrolls up to read"
    - "Progress bars use spring physics animations"
    - "All buttons have hover/press micro-interactions"
    - "Loading states show skeleton placeholders"
    - "Page transitions use smooth fade/slide animations"
  artifacts:
    - path: "src/components/install/step-install.tsx"
      provides: "Fully enhanced installation step with all UX improvements"
      contains: ["DockerLogViewer", "LayerProgress", "enhanced Progress", "enhanced Button", "Skeleton", "page transition props"]
  key_links:
    - from: "src/components/install/step-install.tsx"
      to: "src/components/ui/log-viewer.tsx"
      via: "import statement"
      pattern: "from '@/components/ui/log-viewer'"
    - from: "src/components/install/step-install.tsx"
      to: "src/components/ui/layer-progress.tsx"
      via: "import statement"
      pattern: "from '@/components/ui/layer-progress'"
    - from: "src/components/install/step-install.tsx"
      to: "src/components/ui/progress.tsx"
      via: "import statement"
      pattern: "from '@/components/ui/progress'"
    - from: "src/components/install/step-install.tsx"
      to: "src/components/ui/button.tsx"
      via: "import statement"
      pattern: "from '@/components/ui/button'"
    - from: "src/components/install/step-install.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import statement"
      pattern: "from '@/components/ui/skeleton'"
    - from: "src/components/install/step-install.tsx"
      to: "src/App.tsx"
      via: "route component usage"
      pattern: "export default function.*Page"
---
# Plan 08: Final Installation Step Integration

## Objective
Integrate all UX enhancements from previous plans into the installation step to create a cohesive, polished installation experience. This plan ensures all components work together seamlessly and that the installation flow delivers on all phase 7 requirements.

**Purpose:** Verify that the installation step correctly implements real-time Docker logs, per-layer progress, spring physics animations, button micro-interactions, skeleton loading states, and page transitions as a unified whole.

**Output:** A fully enhanced installation step that integrates all phase 7 UX improvements and serves as the final verification point for the phase.

## Execution Context
@/home/nlg/projects/openclaw-installer/.opencode/get-shit-done/workflows/execute-plan.md
@/home/nlg/projects/openclaw-installer/.opencode/get-shit-done/templates/summary.md

## Context
@.planning/phases/07-installation-ux-animation-foundation/07-CONTEXT.md
@.planning/phases/07-installation-ux-animation-foundation/07-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@src/components/install/step-install.tsx (to be verified)
@src/components/ui/log-viewer.tsx
@src/components/ui/layer-progress.tsx
@src/components/ui/progress.tsx
@src/components/ui/button.tsx
@src/components/ui/skeleton.tsx
@src/App.tsx

<interfaces>
From src/components/ui/log-viewer.tsx:
```typescript
export function useDockerLogs(): {
  logs: string;
  containerRef: MutableRefObject<HTMLDivElement | null>;
};

export function DockerLogViewer(): JSX.Element;
```

From src/components/ui/layer-progress.tsx:
```typescript
export function useDockerLayerProgress(): {
  layers: Array<{ id: string; description: string; percentage: number }>;
};

export function LayerProgress(): JSX.Element;
```

From src/components/ui/progress.tsx:
```typescript
export function Progress({ value, className }: ProgressProps): JSX.Element;
// Internally uses motion.div with spring physics variants
```

From src/components/ui/button.tsx:
```typescript
export const Button: React.ComponentType<ButtonProps>;
// Internally uses motion.button with Framer Motion gesture props
```

From src/components/ui/skeleton.tsx:
```typescript
export { Skeleton } from "@radix-ui/react-skeleton";
```

From src/App.tsx:
```typescript
import { AnimatePresence } from "motion/react";
// Wraps the outlet/content area
```
</interfaces>

## Tasks

<task type="auto">
  <name>Task 1: Verify Installation Step Integration</name>
  <files>src/components/install/step-install.tsx</files>
  <action>
  Verify src/components/install/step-install.tsx:
  1. Confirm it imports and uses DockerLogViewer
  2. Confirm it imports and uses LayerProgress
  3. Confirm it uses the enhanced Progress component (with motion.div)
  4. Confirm it uses the enhanced Button component (with motion.button)
  5. Confirm it imports and uses Skeleton for loading states (if applicable during installation)
  6. Confirm it has page transition props (initial, animate, exit, transition) if it's a page component
  7. Ensure the layout properly displays all components during installation
  8. Verify no console errors or warnings related to missing imports or components
  </action>
  <verify>
  <automated>
  grep -r "DockerLogViewer" src/components/install/step-install.tsx && \
  grep -r "LayerProgress" src/components/install/step-install.tsx && \
  grep -r "import.*Progress.*from.*'@/components/ui/progress'" src/components/install/step-install.tsx && \
  grep -r "import.*Button.*from.*'@/components/ui/button'" src/components/install/step-install.tsx && \
  grep -r "import.*Skeleton.*from.*'@/components/ui/skeleton'" src/components/install/step-install.tsx && \
  grep -r "initial=" src/components/install/step-install.tsx && \
  grep -r "animate=" src/components/install/step-install.tsx && \
  grep -r "exit=" src/components/install/step-install.tsx && \
  grep -r "transition=" src/components/install/step-install.tsx
  </automated>
  </verify>
  <done>
  Installation step correctly imports and uses all enhanced components from phase 7 plans, demonstrating full integration of UX improvements
  </done>
</task>

</tasks>

## Verification
After all tasks complete:
```bash
# Verify all imports and usage
grep -r "DockerLogViewer" src/components/install/step-install.tsx
grep -r "LayerProgress" src/components/install/step-install.tsx
grep -r "import.*Progress.*from.*'@/components/ui/progress'" src/components/install/step-install.tsx
grep -r "import.*Button.*from.*'@/components/ui/button'" src/components/install/step-install.tsx
grep -r "import.*Skeleton.*from.*'@/components/ui/skeleton'" src/components/install/step-install.tsx
grep -r "initial=" src/components/install/step-install.tsx
grep -r "animate=" src/components/install/step-install.tsx
grep -r "exit=" src/components/install/step-install.tsx
grep -r "transition=" src/components/install/step-install.tsx
```

## Success Criteria
- [ ] Installation step shows DockerLogViewer and LayerProgress during installation
- [ ] Installation step uses enhanced Progress component with spring physics
- [ ] Installation step uses enhanced Button component with micro-interactions
- [ ] Installation step shows skeleton placeholders during loading states (if applicable)
- [ ] Installation step has page transition props for smooth enter/exit animations
- [ ] All imports are correct and no missing component errors

## Output
After completion, create `.planning/phases/07-installation-ux-animation-foundation/07-08-SUMMARY.md`