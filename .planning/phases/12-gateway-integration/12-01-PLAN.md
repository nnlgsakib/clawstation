---
phase: 12-gateway-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/setup-wizard.tsx
  - src/components/wizard/welcome-step.tsx
  - src/components/wizard/model-step.tsx
  - src/components/wizard/api-keys-step.tsx
  - src/components/wizard/sandbox-step.tsx
  - src/components/wizard/channels-step.tsx
  - src/components/wizard/review-step.tsx
  - src/stores/use-wizard-store.ts
  - src/hooks/use-wizard.ts
  - src/router.tsx
autonomous: true
requirements:
  - GW-01
  - GW-02
  - GW-03
  - GW-04
  - GW-05

must_haves:
  truths:
    - "User sees a multi-step setup wizard on first launch"
    - "User selects model provider (Anthropic, OpenAI, Google, OpenRouter) with visual cards"
    - "User enters API key for selected provider with validation feedback"
    - "User configures sandbox settings (mode, scope, workspace access)"
    - "User selects channels to enable (WhatsApp, Telegram, Discord, Slack)"
    - "User reviews all settings before installation begins"
    - "Wizard saves config to openclaw.json format for Gateway"
  artifacts:
    - path: "src/pages/setup-wizard.tsx"
      provides: "Wizard page with step navigation and progress bar"
      min_lines: 80
    - path: "src/stores/use-wizard-store.ts"
      provides: "Zustand store for wizard state (current step, selections)"
      exports: ["useWizardStore"]
    - path: "src/components/wizard/model-step.tsx"
      provides: "Model provider selection with cards and logos"
      min_lines: 60
    - path: "src/components/wizard/api-keys-step.tsx"
      provides: "API key entry with provider-specific validation"
      min_lines: 50
    - path: "src/components/wizard/sandbox-step.tsx"
      provides: "Sandbox configuration (mode, scope, workspace access)"
      min_lines: 60
    - path: "src/components/wizard/channels-step.tsx"
      provides: "Channel selection with enable/disable toggles"
      min_lines: 60
    - path: "src/components/wizard/review-step.tsx"
      provides: "Review all settings before install"
      min_lines: 50
  key_links:
    - from: "src/stores/use-wizard-store.ts"
      to: "src/pages/setup-wizard.tsx"
      via: "Zustand store consumed by wizard page"
      pattern: "useWizardStore"
    - from: "src/components/wizard/review-step.tsx"
      to: "commands config"
      via: "invoke write_config with generated openclaw.json"
      pattern: "invoke.*write_config"
---

<objective>
Create the config-first setup wizard UI

Purpose: Users must configure all OpenClaw settings (model provider, API keys, sandbox, channels) BEFORE installation starts. This is the entry point of the redesigned app.
Output: Multi-step wizard with 6 steps: Welcome, Model Provider, API Keys, Sandbox, Channels, Review & Install
</objective>

<execution_context>
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/12-gateway-integration/12-CONTEXT.md
@.planning/12-gateway-integration/12-RESEARCH.md

# Existing patterns to follow
@src/pages/configure.tsx
@src/stores/use-config-store.ts
@src/hooks/use-config.ts
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/skeleton.tsx
</context>

<interfaces>
From src/stores/use-config-store.ts:
```typescript
export interface OpenClawConfig {
  agents?: { defaults?: { model?: string | { primary?: string; fallbacks?: string[] }; sandbox?: { mode?: string; scope?: string; backend?: string } } };
  channels?: Record<string, { enabled?: boolean; botToken?: string; allowFrom?: string[]; dmPolicy?: string }>;
  gateway?: { port?: number; bind?: string };
}
```

From src/hooks/use-config.ts:
```typescript
export function useConfig(): UseQueryResult<OpenClawConfig, Error>
export function useSaveConfig(): UseMutationResult<void, Error, OpenClawConfig>
```

From src/components/ui/card.tsx:
```typescript
export function Card({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
export function CardHeader({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
export function CardTitle({ className, ...props }: React.HTMLAttributes<HTMLHeadingElement>)
export function CardDescription({ className, ...props }: React.HTMLAttributes<HTMLParagraphElement>)
export function CardContent({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create wizard Zustand store</name>
  <files>src/stores/use-wizard-store.ts</files>
  <read_first>src/stores/use-config-store.ts</read_first>
  <behavior>
    - Store tracks: currentStep (0-5), modelProvider, apiKey, sandboxMode, sandboxScope, workspaceAccess, selectedChannels, channelConfigs
    - Store exposes: nextStep(), prevStep(), goToStep(), setModelProvider(), setApiKey(), setSandboxConfig(), toggleChannel(), setChannelConfig(), getGeneratedConfig(), reset()
    - getGeneratedConfig() returns OpenClawConfig object matching openclaw.json schema
    - Model providers: { id: "anthropic", name: "Anthropic", models: ["claude-opus-4-6", "claude-sonnet-4-6"] }, { id: "openai", name: "OpenAI", models: ["gpt-5.2", "o3"] }, { id: "google", name: "Google", models: ["gemini-2.5-pro"] }, { id: "openrouter", name: "OpenRouter", models: ["anthropic/claude-opus-4-6", "openai/gpt-5.2"] }
    - Default selections: modelProvider="anthropic", sandboxMode="non-main", sandboxScope="session", workspaceAccess="none", selectedChannels=[]
    - Total steps: 6 (Welcome=0, Model=1, API Keys=2, Sandbox=3, Channels=4, Review=5)
  </behavior>
  <action>
    Create src/stores/use-wizard-store.ts following the pattern from use-config-store.ts.
    Use Zustand with create() (no middleware needed — wizard state is ephemeral, not persisted).
    Define interfaces: WizardState, ModelProvider, ChannelOption.
    Export useWizardStore hook.
    The MODEL_PROVIDERS constant should be an array of ModelProvider objects with id, name, models array, keyFormat (regex for validation), and keyPlaceholder.
    The CHANNEL_OPTIONS constant should be an array of ChannelOption objects with id, name, icon (lucide icon name), fields (array of {key, label, type, placeholder} for channel-specific config).
  </action>
  <verify>npm run build succeeds with no TypeScript errors</verify>
  <done>Wizard store exists with all state management functions and generated config output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create wizard page and step components</name>
  <files>src/pages/setup-wizard.tsx, src/components/wizard/welcome-step.tsx, src/components/wizard/model-step.tsx, src/components/wizard/api-keys-step.tsx, src/components/wizard/sandbox-step.tsx, src/components/wizard/channels-step.tsx, src/components/wizard/review-step.tsx</files>
  <read_first>src/pages/configure.tsx, src/components/ui/card.tsx, src/components/ui/button.tsx</read_first>
  <behavior>
    - setup-wizard.tsx: Progress bar at top (6 steps), renders current step component, Next/Back buttons, step titles
    - welcome-step.tsx: "Welcome to OpenClaw Desktop" heading, brief description, "Get Started" button
    - model-step.tsx: Grid of model provider cards (Anthropic, OpenAI, Google, OpenRouter) with logo placeholder, name, description. Click to select. Selected card has border highlight. Below cards: model dropdown for selected provider.
    - api-keys-step.tsx: Shows selected provider name, API key input (password type), key format hint, "Validate" button (placeholder for now)
    - sandbox-step.tsx: Three radio groups — Mode (off/non-main/all), Scope (session/agent/shared), Workspace Access (none/ro/rw). Each option has label and description.
    - channels-step.tsx: List of channel cards with toggle switch. Each card shows channel name, icon, brief description. When enabled, shows channel-specific fields (bot token input for Telegram/Discord, allow-from list for WhatsApp).
    - review-step.tsx: Summary of all selections organized by category. "Start Installation" button.
    - All steps use shadcn/ui Card components and Lucide icons
    - Steps consume useWizardStore for state
  </behavior>
  <action>
    Create the wizard page and all 6 step components.
    The wizard page (setup-wizard.tsx) should:
    - Import and use useWizardStore
    - Show a progress indicator (step dots or numbered bar) at the top
    - Render the current step component based on wizardStore.currentStep
    - Show Back/Next buttons at the bottom (Back hidden on step 0)
    - On final step, "Start Installation" button invokes write_config with generated config
    - Use AnimatePresence for step transitions

    Each step component should:
    - Be a named export function
    - Use useWizardStore() to read/write state
    - Use shadcn/ui components (Card, Button, Input, RadioGroup, Switch)
    - Use Lucide React icons

    The router.tsx should be updated to add:
    - Import SetupWizard
    - Route: path="/setup" element={SetupWizard}
    - Redirect logic: if no config exists, redirect to /setup (handled in dashboard)
  </action>
  <verify>npm run build succeeds, /setup route renders wizard</verify>
  <done>All wizard steps render correctly with state management and navigation</done>
</task>

<task type="auto">
  <name>Task 3: Update router and dashboard to show wizard on first launch</name>
  <files>src/router.tsx, src/pages/dashboard.tsx</files>
  <read_first>src/router.tsx, src/pages/dashboard.tsx</read_first>
  <action>
    In src/router.tsx:
    - Add import for SetupWizard from "@/pages/setup-wizard"
    - Add route: `<Route path="/setup" element={<SetupWizard />} />`

    In src/pages/dashboard.tsx:
    - Import useConfig hook
    - If config query returns error (no config file) or config is empty/null, show "Complete Setup" card with Link to /setup
    - If config exists and Gateway is not running, show "Start Gateway" card with Link to /install or inline start button
    - Keep existing status cards when Gateway is running
  </action>
  <verify>npm run build succeeds, dashboard shows setup link when no config</verify>
  <done>Dashboard correctly routes to setup wizard when config is missing</done>
</task>

</tasks>

<verification>
- [ ] Wizard store manages all state correctly
- [ ] All 6 step components render with proper UI
- [ ] Navigation between steps works (Next/Back)
- [ ] Progress bar updates correctly
- [ ] Config generation produces valid openclaw.json structure
- [ ] Router includes /setup route
- [ ] Dashboard shows setup link when no config exists
</verification>

<success_criteria>
- User can navigate through all 6 wizard steps
- Model provider selection works with visual feedback
- API key entry works with format hints
- Sandbox configuration produces correct config values
- Channel selection enables/disables channels
- Review step shows all selections
- Generated config matches OpenClaw openclaw.json schema
- Dashboard detects missing config and offers setup wizard
</success_criteria>

<output>
After completion, create `.planning/phases/12-gateway-integration/12-01-SUMMARY.md`
</output>
