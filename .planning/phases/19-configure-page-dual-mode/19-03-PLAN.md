---
phase: 19-configure-page-dual-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/pages/configure.tsx
autonomous: false
requirements:
  - CONF-FIX-01
  - CONF-FIX-02
  - CONF-FIX-03
user_setup: []

must_haves:
  truths:
    - "Config loads correctly from both file (useConfig) and gateway (useGatewayConfig)"
    - "Save flow validates before writing, shows toast on success/failure"
    - "Mode toggle preserves state when switching between UI and JSON"
    - "Dirty indicator works across both modes"
    - "Error states display correctly (load error, save error, validation error, JSON parse error)"
  artifacts:
    - path: "src/pages/configure.tsx"
      provides: "Fully integrated dual-mode config editor with save & restart"
      modifies: "existing file"
  key_links:
    - from: "src/pages/configure.tsx"
      to: "src/hooks/use-config.ts"
      via: "useConfig for initial load, useSaveConfig for file save, useValidateConfig for validation"
      pattern: "use(Config|SaveConfig|ValidateConfig)"
    - from: "src/pages/configure.tsx"
      to: "src/hooks/use-gateway.ts"
      via: "useGatewayConfig for gateway config, useGatewayConfigPatch for hot-reload save"
      pattern: "useGateway(Config|ConfigPatch)"
---

<objective>
Integration verification and polish for the dual-mode configure page.

Purpose: Ensure the full flow works end-to-end: load config, edit in UI or JSON mode, save (file or gateway), restart gateway. Fix any integration issues between the new JSON editor, mode toggle, restart button, and existing config data flow.

Output: Polished configure.tsx with all features working together, no regressions.
</objective>

<execution_context>
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/workflows/execute-plan.md
@D:/projects/rust/openclaw-ins/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-configure-page-dual-mode/19-CONTEXT.md
@.planning/phases/19-configure-page-dual-mode/19-01-SUMMARY.md
@.planning/phases/19-configure-page-dual-mode/19-02-SUMMARY.md
@src/pages/configure.tsx
@src/components/config/json-editor.tsx
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Checkpoint: Verify full configure page flow</name>
  <files>src/pages/configure.tsx, src/components/config/json-editor.tsx, src/pages/monitor.tsx</files>
  <action>
    Human verification of the complete dual-mode configure page:
    - UI Mode: existing section-based editing (provider, sandbox, tools, agents, dynamic sections)
    - JSON Mode: full config JSON editor with syntax highlighting and inline error display
    - Mode toggle between UI and JSON
    - Save Changes button (saves via gateway patch or file write)
    - Save & Restart button (saves + navigates to monitor + auto-restarts)
    - Gateway status banner when disconnected
    - Dirty state indicator
  </action>
  <verify>
    1. Start the app: `pnpm tauri dev`
    2. Navigate to `/configure`
    3. Verify UI Mode toggle is visible and works
    4. In UI Mode: change a provider setting → verify Save Changes button enables
    5. Click Save Changes → verify toast shows success
    6. Switch to JSON Mode → verify full config JSON is shown in textarea
    7. Edit the JSON (e.g., change a value) → verify no parse errors
    8. Introduce invalid JSON (delete a closing brace) → verify inline error appears
    9. Fix the JSON → verify error clears
    10. Save from JSON mode → verify toast success
    11. Switch back to UI Mode → verify changes are reflected
    12. If gateway connected: verify "Save & Restart" button is visible
    13. Click "Save & Restart" → verify navigates to monitor page
    14. On monitor page → verify restart is auto-triggered (restart button shows loading state)
  </verify>
  <done>
    - All verification steps pass
    - Type "approved" or describe issues found
  </done>
</task>

<task type="auto">
  <name>Task 1: Fix any integration issues from checkpoint</name>
  <files>src/pages/configure.tsx, src/components/config/json-editor.tsx</files>
  <read_first>
    src/pages/configure.tsx
    src/components/config/json-editor.tsx
  </read_first>
  <action>
    Address any issues reported during the human verification checkpoint.

    Common issues to check and fix:
    1. If JSON mode doesn't reflect store changes: sync local JSON text when storeConfig changes (add useEffect to update local state when storeConfig reference changes)
    2. If mode switch loses edits: ensure JSON-to-UI switch updates the store with parsed JSON before rendering UI mode
    3. If save from JSON mode doesn't work: verify the `onSave` callback properly calls `setConfig` before `handleSave`
    4. If "Save & Restart" doesn't save: check that `handleSaveAndRestart` awaits the save mutation before navigating
    5. If monitor doesn't auto-restart: check that `pendingRestart` is read before being reset, and the timeout fires
    6. If dirty state doesn't update in JSON mode: ensure `setConfig` is called which sets `isDirty: true` by default (verify in store)

    Only make changes that were specifically called out in the checkpoint. Do not refactor working code.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - All checkpoint issues resolved
    - TypeScript compiles cleanly
    - No regressions in existing configure page functionality
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `pnpm build` succeeds without errors
3. Human verification checkpoint approved
4. Full flow tested: load → edit (UI) → edit (JSON) → save → restart
</verification>

<success_criteria>
- [ ] Checkpoint approved by human
- [ ] All integration issues fixed
- [ ] TypeScript compiles cleanly
- [ ] Build succeeds
- [ ] UI mode and JSON mode both functional
- [ ] Save works in both modes
- [ ] Save & Restart flow completes end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/19-configure-page-dual-mode/19-03-SUMMARY.md`
</output>
